import React, { useState } from "react";
import { Container, Row, Col } from "reactstrap";
import chooseImg from "../../assests/images/why-choose-us.png";
import "./choose-us.css";
import ReactPlayer from "react-player";

const ChooseUs = () => {
  const [showVideo, setShowVideo] = useState(false);
  return (
    <section>
      <Container>
        <Row>
          <Col lg="6" md="6">
            <div className="choose__content">
              <h2>Why Choose Us</h2>
              <p>
                The virtual schools and courses we provide are marketable,up to date with the current demand in  the job market.We offer virtual learning that mimic physical environment. We have one of the best educators who provide one on one studies guranteeing success.We are ready to meet you where you are offering the best services.Embarking on a learning journey with us gurantees wholesome learning.
              </p>
            </div>
          </Col>

          <Col lg="6" md="6">
            <div className="choose__img">
              {showVideo ? (
                <ReactPlayer
                  url="https://www.youtube.com/watch?v=qFp27TR4Yew"
                  controls
                  width="100%"
                  height="350px"
                />
              ) : (
                <img src={chooseImg} alt="" className="w-100" />
              )}

              {!showVideo && (
                <span className="play__icon">
                  <i
                    class="ri-play-circle-line"
                    onClick={() => setShowVideo(!showVideo)}
                  ></i>
                </span>
              )}
            </div>
          </Col>
        </Row>
      </Container>
    </section>
  );
};

export default ChooseUs;
